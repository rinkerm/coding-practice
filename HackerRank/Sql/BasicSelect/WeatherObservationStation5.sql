--LANGUAGE = MySQL
(
	SELECT CITY, 
		LENGTH(CITY) 
	FROM STATION  
	ORDER BY LENGTH(CITY) asc, 
		CITY ASC 
	LIMIT 0,1)
UNION ALL

(
	SELECT CITY, 
		LENGTH(CITY) 
	FROM STATION  
	ORDER BY LENGTH(CITY) DESC, 
		CITY ASC 
	LIMIT 0,1);
